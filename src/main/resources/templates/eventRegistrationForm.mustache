<div class="modal-header">
    <h5 class="modal-title" id="eventRegisterLabel">🎉 이벤트 만들기</h5>
    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
</div>
<div class="modal-body">
    <form id="eventForm">
        <div class="form-group mb-3">
            <label for="eventItem" class="form-label">이벤트명<span class="required-true">*</span></label>
            <select class="form-select" id="eventItem" required>
                <option value="TEAM_PARTY" selected>회식</option>
                <option value="UNIFORM">유니폼</option>
                <option value="MEMBERSHIP_TRAINING">엠티</option>
            </select>
        </div>
        <div class="form-group mb-3">
            <label for="amount" class="form-label">금액<span class="required-true">*</span></label>
            <input type="number" class="form-control" id="amount" placeholder="얼마예요?" required>
        </div>
        <div class="form-group mb-3">
            <label for="dueDate" class="form-label">만료일<span class="required-true">*</span></label>
            <input type="date" class="form-control" id="dueDate" required>
        </div>
        <div class="form-group mb-3">
            <label for="description" class="form-label">이벤트 설명<span class="required-false"></span></label>
            <textarea class="form-control form-control-lg" id="description" rows="5" placeholder="설명이 필요하다면 적어주세요!"></textarea>
        </div>
        <div class="form-group mb-3">
            <label for="status" class="form-label">활성화 여부<span class="required-true">*</span></label>
            <div class="form-check form-switch">
                <input class="form-check-input" type="checkbox" id="status">
                <label class="form-check-label" id="statusLabel" for="status">off</label>
            </div>
        </div>

        <div class="d-flex justify-content-center">
            <button type="submit" class="btn btn-primary mt-3" id="registerEventButton">등록</button>
        </div>
    </form>
</div>
